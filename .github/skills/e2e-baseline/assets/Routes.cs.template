namespace {{Namespace}}.Config;
 
/// <summary>
/// URL route constants derived from static analysis of the pre-migration web project.
/// Pre-migration: .aspx routes generated from the page-inventory.json.
/// Post-migration: update these values to the Razor Pages routes and re-run dotnet test.
/// </summary>
public static class Routes
{
    // Route constants are generated by e2e-baseline-test-generator.
    // One constant per in-scope page from page-inventory.json.
    // Format: public static string {PageName} => $"{TestSettings.BaseUrl}/{UrlPattern}";
}